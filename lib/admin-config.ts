/**
 * Admin panel configuration
 * The slug is intentionally non-obvious to prevent guessing.
 */
export const ADMIN_SLUG = "architect";
export const ADMIN_PATH = `/${ADMIN_SLUG}`;
export const ADMIN_API_PATH = `/api/${ADMIN_SLUG}`;
